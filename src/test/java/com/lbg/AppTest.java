package com.lbg;

import org.junit.jupiter.api.Test;

import java.awt.*;

import static org.junit.jupiter.api.Assertions.assertEquals;

class AppTest {

    @Test
    void main() {
        Grid g = new Grid(3); // hold where everything is in the grid
        IO io = new IO(); // takes the input from the user
        Menu menu = new Menu(); // runs the game and holds moves
        assertEquals(1,1);

    }
}